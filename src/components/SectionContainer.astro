---
const { class: className, id } = Astro.props
---

<section 
  id={id}
  data-section={id}
  class={`section ${className} scroll-m-20 w-full mx-auto container px-4 sm:px-6 md:px-8 lg:px-10 max-w-[850px] py-6 lg:py-10`}
>
  <slot />
</section>